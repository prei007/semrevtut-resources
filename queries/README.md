This folder holds the SPARQL query files. 

ARQ needs to be installed. From the command line, and while in the directory `semrevtut-resources`, a typical ARQ command looks like this:

```
arq --data=studies/Millet2013_annotations.trig \
    --query=queries/list-millet-annotations.rq
```
